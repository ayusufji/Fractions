package code;

public class Runner
{
    public static void main(String[] args)
    {

        /**
        Fraction myFraction = new Fraction (2,3);
        Fraction otherFraction = new Fraction (5);
        Fraction a = new Fraction(myFraction);
        Fraction multiplied = myFraction.multiply(otherFraction);
        Fraction divided = myFraction.divide(otherFraction);
        Fraction inv = myFraction.inverse();
        System.out.println(inv);

        Fraction f1 = null;
        try
        {
            Fraction f1 = new Fraction(3,0);
        }
        catch(Exception e)
        {
            System.out.println("Fractions cannot have a zero denominator");
        }
        System.out.print(f1);
        */

        Fraction thatFraction = new Fraction(12 , 4);
        System.out.print(thatFraction);

    }

}
